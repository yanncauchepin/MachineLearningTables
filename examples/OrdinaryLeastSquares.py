import matplotlib.pyplot as plt
import pandas as pd
import numpy as np

"""ORDINARY LEAST SQUARES (OLS) OR LINEAR LEAST SQUARES"""
"""
Using the ordinary least squares (OLS) method (sometimes also called linear least
squares) to estimate the parameters of the linear regression line that minimizes
the sum of the squared vertical distances (residuals or errors) to the training
examples.
"""
